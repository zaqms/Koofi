/**
 * TEMPORARY experiment (Amjad): 308 `/` + `/en` to Most Popular.
 *
 * P0 home (Amjad GREENLIGHT) is the live `/` chrome — keep this **false**.
 * Flip to `true` only to restore the bounce-rate redirect. Do not merge
 * that flip with the P0 home PR.
 */
export const TEMPORARY_DEFAULT_LANDING_MOST_POPULAR = false;
